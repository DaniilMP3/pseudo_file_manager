[tool.poetry]
name = "googledrive-cli"
version = "0.1.0"
description = ""
authors = ["DaniilMP3 <daniilpurisov390@gmail.com>"]
readme = "README.md"
packages = [{include = "googledrive_cli"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.scripts]
main = "googledrive_cli.main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
